{
  "name": "@sqlrag/bank-reconcile",
  "version": "1.0.0",
  "description": "Bank Reconciliation — SAM plugin (foundation, endpoints in progress).",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sqlrag/sam-shared": "*",
    "express": "^4.19.2",
    "knex": "^3.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "sqlite3": "^6.0.1",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
